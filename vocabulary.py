def check_and_get_vocab(args):
    args.folder
    # TODO: check if vocabulary is stored in given folder then load it, else construct it
    return 1


class Vocabulary:
    def __init__(self, max_voc_size=None):
        self.max_voc_size = max_voc_size